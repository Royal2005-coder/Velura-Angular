import { Component } from '@angular/core';

/**
 * Placeholder for the `/` route. `adminHomeRedirectGuard` sends the role to its first module.
 */
@Component({
  selector: 'app-admin-home-redirect',
  template: '',
})
export class AdminHomeRedirectPage {}
