## Jira
- Key: KAN-___
- Team: Storefront / Admin / Lead
- Link: https://webadvance.atlassian.net/browse/KAN-___

## Summary
- Why this change exists (one paragraph). File list is generated by CI on this MR.

## Target
- Feature work: **develop** (staging). Never `main`.
- Promote: source **develop** → target **main** only after staging verify.

## Checks
- [ ] Title starts with `KAN-n`
- [ ] HTTP stays in services; no `any`; Signals for derived UI
- [ ] `test:api` / `test:ng` covered for touched area
- [ ] No new `docs/*.md` for this ticket (source-of-truth maps only)

## Architecture (only if layers/contracts change)
- Decision in this description (not a new ADR file unless it is a lasting rule):

## Production
Feature MR does not deploy. `develop` = staging. `main` = production.
This MR **is** the version log: git commits + CI trace note + Changes tab.
